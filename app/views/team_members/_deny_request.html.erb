<%= form_for current_participant.proposal.pendings.find(pending_id), url: team_member_path(pending_id),
	html: {method: :delete}, remote: true, class: 'pending-form' do |f| %>
	<%= f.submit "Deny Request", class: 'deny-btn' %>
<% end %> 