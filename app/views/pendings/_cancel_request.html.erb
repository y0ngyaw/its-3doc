<%= form_for(current_participant.pendings.find_by(proposal_id: proposal_id), 
		html: {method: :delete}, remote: true) do |f| %>
	<%= f.submit "Cancel request", class: 'proposal-cancel-req cancel-req-btn' %>
<% end %>